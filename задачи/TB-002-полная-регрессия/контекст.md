# Контекст: TB-002-полная-регрессия

- **Код задачи:** TB-002-полная-регрессия
- **Версия памяти:** v001 от 2026-09-15
- **Статус:** в работе

## Актуальное состояние

Планируется полный просмотр кода (11 файлов, 1501 строка) и прогон
функциональных проверок. Среда: локальный Python 3.14.7 + `.venv` проекта
(все зависимости), сеть без VPN (Google и bigmodel доступны; Telegram/Render
недоступны — соответствующие пункты будут помечены).

## Подтверждённые факты

- Репетиция регрессии (TBot2, до удаления): py_compile OK, импорт OK,
  таблицы читаются, классификация GLM 5/5.
- Код TBot идентичен коду репетиции (git-хеши), поэтому ожидаются те же
  результаты; проверка всё равно выполняется полностью на TBot.

## Принятые решения

- Контролируемая тестовая запись в таблицы: одна тестовая строка расхода и
  одна задача с пометкой «ТЕСТ», чтение, удаление той же сессией через
  gspread (у бота функций удаления нет — используется прямой вызов API).
- Поисковый конвейер проверяется `scripts/test_search.py` (без записи в
  таблицы, как задумано автором скрипта).

## Целевые файлы и компоненты

- Чтение: `bot.py` (575), `llm.py` (287), `sheets.py` (157),
  `zakup_sheets.py` (108), `config.py` (50), `web/dashboard.py` (114),
  `searchers/*` (180), `scripts/test_search.py` (30).

## Зависимости и контракты

- Критерии проверки: `критерии_приёмки.md` С1–С9 + минимальный чек-лист.

## От родителей (наследование контекста, § 3.2)

- TB-001: код не менялся при переносе методологии — регрессия должна это
  подтвердить и зафиксировать протокол.

## Риски и открытые вопросы

- Пункты, требующие Telegram/Render (вебхук, живой деплой), будут помечены
  «не применимо офлайн» — для них нужно включение VPN, отдельным шагом.

## Что читал (обязательный контекст, § 3.6)

- `критерии_приёмки.md`, `артефакты/tbot.json`, вся структура кода
  (перечислена выше) — по ходу проверки.

## Следующий разумный шаг

Прочитать bot.py полностью, затем остальные модули; зафиксировать
замечания; прогнать офлайн- и живые проверки; записать протокол и вердикт.
