# Контекст: TB-003-фиксы-регрессии

- **Код задачи:** TB-003-фиксы-регрессии
- **Версия памяти:** v001 от 2026-09-15
- **Статус:** в работе

## Актуальное состояние

Дефекты из протокола TB-002 утверждены к исправлению Александром (15.09.2026,
«выполняем все необходимые изменения… не спрашивая»). VPN включён — пункты,
заблокированные сетью (GLM, getWebhookInfo, деплой), входят в объём.

## Подтверждённые факты

- Дефекты и их точные места — в протоколе TB-002 (`../TB-002-полная-регрессия/контекст.md`).
- Push: `git push origin main` (PAT вшит в remote URL); деплой: POST
  /v1/services/srv-dai4pcu743jc73dr7uqg/deploys, ожидание live, затем
  getWebhookInfo + /health.

## Принятые решения

- Фикс edit_pick — семантика «новое сообщение вместо кнопки отменяет
  ожидание» (как уже сделано для voice_confirm): единая строка-проверка
  `in ("voice_confirm", "edit_pick")`.
- Фикс дашборда — сравнение пароля в байтах (`.encode("utf-8")`) с обеих
  сторон.
- Мелочи из протокола TB-002 правятся тем же коммитом (неиспользуемые
  переменные, защита close_task).

## Целевые файлы и компоненты

- `bot.py`: on_voice (uid), process_text (отмена edit_pick), task_done
  (защита int), apply_answer (мусорная d2), except e (bot.py:263).
- `web/dashboard.py`: login_post (сравнение пароля в байтах).

## Зависимости и контракты

- `критерии_приёмки.md` — регресс после фиксов обязателен (правило Александра).

## От родителей (наследование контекста, § 3.2)

- TB-002: дефекты подтверждены протоколом; код до фиксов не менялся.

## Риски и открытые вопросы

- Риск один: задеть код вне точек фикса. Минимизируется точечными правками
  и py_compile + симуляциями до пуша.

## Что читал (обязательный контекст, § 3.6)

- `bot.py`, `web/dashboard.py` (полностью), протокол TB-002,
  `артефакты/tbot.json` (delivery).

## Следующий разумный шаг

Внести правки, прогнать локальную регрессию, закоммитить, пуш+деплой,
послепроверка, закрыть узел.
