# Постановка: TB-003-фиксы-регрессии

- **Код задачи:** TB-003-фиксы-регрессии
- **Родитель (parentId):** TB-002-полная-регрессия
- **Дата постановки:** 2026-09-15

## Что нужно получить

Три дефекта, найденные регрессией TB-002, исправлены; код восстановленной
функциональности проверен; обновление доставлено на Render (push + деплой +
послепроверка). Подтверждение Александра: «выполняем все необходимые
изменения… не спрашивая меня» (15.09.2026, чат).

## Зачем это нужно

1. Голосовой ввод фактически не работает: `on_voice` падает NameError после
   успешной расшифровки.
2. Неверный пароль кириллицей на дашборде даёт HTTP 500 вместо формы ошибки.
3. Текст вместо кнопки в диалоге правки строки даёт KeyError и тишину.

## Границы работы

- Входит: точечные фиксы (суммарно ~8 строк в `bot.py` и `web/dashboard.py`),
  включая мелочи из протокола TB-002 (неиспользуемые переменные, защита
  close_task от нечислового номера); локальная регрессия; живые проверки
  (GLM, поисковый конвейер, getWebhookInfo); push + деплой + послепроверка.
- НЕ входит: новые функции; изменения, не связанные с найденными дефектами;
  запись в боевые таблицы (кроме контролируемой тестовой, как в TB-002).

## Ожидаемый результат

- Симуляция on_voice: диалог voice_confirm создаётся, кнопки формируются.
- Текст в диалоге edit_pick: диалог корректно отменяется, исключений нет.
- Неверный пароль кириллицей: форма «неверный пароль» (200), не 500.
- Чек-лист `критерии_приёмки.md`: все 5 пунктов пройдены, включая деплой.

## Ограничения

- Поставка — push в main + деплой Render по runbook из паспорта; WEBHOOK_SECRET
  не трогать; env-переменные на Render не менять.

## Критерии приёмки

1. Все три дефекта больше не воспроизводятся (симуляции/живые запросы).
2. py_compile, импорт, kb() — без ошибок; классификация GLM 5/5 живьём.
3. Деплой в статусе live, getWebhookInfo: url на месте, last_error пуст,
   /health отвечает.
4. Протокол и changeset в `контекст.md`.
