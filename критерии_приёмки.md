# Критерии приёмки TBot — сценарии регрессионной проверки

**Правило (заверил Александр 14.09.2026):** после ЛЮБЫХ доработок кода
и деплоя — пройтись по сценариям этого файла и убедиться, что главные
функции не сломаны. Файл обновляется при каждом изменении функционала.
Отдельно от этого файла живёт паспорт артефакта (`артефакты/tbot.json`):
устройство, инварианты и поставка. Перенос из `docs/FUNCTIONALITY.md`
выполнен задачей TB-001 (2026-09-15).

## Как проводить проверку

1. Проверяющий — отдельный запуск агента, не участвовавший в разработке.
2. Ему выдаются: `задачи/<код>/постановка.md`, этот файл, changeset (diff).
3. Вердикт фиксируется в `контекст.md` узла: **принято / требуется
   доработка / заблокировано / требуется решение архитектора**.

## Сценарии

### С1. Доступ
- Незнакомый пользователь (текст/голос/кнопки) → функций нет; админам
  (ADMIN_IDS) приходит заявка с кнопками «✅ Да, открыть» / «❌ Нет».
- «Да» → строка в лист «Пользователи» (одобрения переживают рестарт),
  пользователю «доступ открыт». «Нет» → «отказано» (до перезапуска бота).
- Пользователи = env USERS ∪ лист «Пользователи» обеих таблиц ∪ config_local.

### С2. Расходы
- «Потратил 5000 на доски для Садовой» → лист «Расходы»: Дата | День недели
  (авто) | Автор | Описание | Сумма | Объект | Категория.
- Категорию подбирает LLM по описанию + похожим прошлым расходам.
- Нет суммы → вопрос «На какую сумму?»; нет объекта → кнопки: последние
  объекты + «➕ Новый объект» → название → создаётся в «Объектах».
- Новый объект создаётся ТОЛЬКО после подтверждения; если вместо названия
  пришёл другой диалог — ничего не создаётся.
- Известный объект подставляется молча (в т.ч. с опечатками — LLM-сопоставление).

### С3. Задачи
- «Серёге позвонить поставщику до пятницы» → лист «Задачи»: № | Дата |
  День недели | Автор | Исполнитель | Задача | Срок | Статус | Объект | Закрыта.
- Нет исполнителя → вопрос «Александр или Сергей?» (синонимы Саня/Серёга
  нормализуются).
- «задача 5 сделана» → Статус «Сделана», дата в «Закрыта».
  Команды /tasks, «запросы» (/zaprosy) — списки.

### С4. Правка строк
- «Измени строку: доски для Парковой на 10000, сумма 12000» → поиск по
  сочетанию ВСЕХ названных признаков (объект И описание И сумма И дата).
- Одна строка → показать целиком + «было → стало». Несколько → кнопки
  выбора. Ноль → «не нашёл, уточни».

### С5. Голосовые
- Голос → «🎤 Расшифровываю…» → «Распознал: … Продолжить?» кнопки «✅/❌».
  Да → текст идёт по обычному пути. Нет → отмена.
- Новое сообщение вместо кнопки → интерпретация отменяется.
- Модель glm-asr-2512; у aiogram Voice НЕТ file_name → getattr.

### С6. Свободное общение
- Сообщения вне учёта (action=none, нет открытого диалога) → ответ через
  llm.chat (кратко, по-русски, без истории). Расход/задача не теряются —
  классификатор в приоритете.

### С7. Поиск поставщиков (ZAKUP)
- «найди бригаду по стяжке в Казани» → бриф (тип/что/город/объём/источник) →
  источник не указан → кнопки (Авито / Яндекс Услуги / весь интернет) →
  коннекторы → LLM-ранжирование → выдача чанками ≤4000 симв.
- Если источник указан в тексте («найди на Авито…») — искать только там,
  кнопки не показываются; кнопки источника — в том же чате, где написан запрос.
- Статус запроса (лист «Запросы»): Новый / Поиск / Есть результаты /
  Нет результатов / Ошибка.
- Запись в таблицу «ZAKUP: поставщики и КП» (Запросы: №|Дата|Автор|Запрос|Тип|
  Что искать|Город|Источники|Статус|Найдено|Примечание; Поставщики:
  № запроса|Название|Описание|Ссылка|Источник|Статус КП|Заметки).
- Новый текст при открытом брифе → если снова search: перезапуск брифа,
  иначе обычная обработка.
- **Известное ограничение**: без YANDEX_SEARCH_API_KEY работает
  glm_web-фолбэк — на ру-запросах даёт мусор/пусто; нормальное качество
  только с ключом Яндекса.

### С8. Веб
- Дашборд: вход по DASHBOARD_PASSWORD (cookie), список запросов +
  поставщики по ссылкам.
- /health — «TBot is alive». /notify?pass=…&text=… → ⚠️-сообщение админам
  в Telegram (ops-канал при VPN-обрывах).
- /tgwebhook, /notify, /health — вне auth-middleware дашборда.

### С9. Инфраструктурные инварианты
- Вебхук: url=https://tbot-j0kj.onrender.com/tgwebhook, секрет в заголовке;
  WEBHOOK_SECRET фиксирован в env — при ротации Telegram получает 403 и
  теряет сообщения; без WEBHOOK_URL — локальный polling.
- Сон бесплатного Render: холодный старт до ~60 сек; сообщение будит инстанс.
- Обновление: git push → POST /v1/services/{id}/deploys → live →
  getWebhookInfo без ошибок (полный runbook — `артефакты/tbot.json`).
- Env на Render — 13 переменных (список в `артефакты/tbot.json` → delivery).

## Минимальный чек-лист после доработок

1. `python -m py_compile bot.py llm.py sheets.py zakup_sheets.py web/dashboard.py searchers/*.py`
2. llm.extract: расход / задача / поиск / none — все четыре классифицируются верно.
3. kb() вызовы без TypeError (с cols и без).
4. Листы: Расходы/Задачи читаются, Пользователи грузятся.
5. Деплой live + getWebhookInfo: url на месте, last_error пуст.
